# DBC 影子中台系統 完整交接文件

> 更新日期：2026/06/08
> 目的：讓接手同事可以獨立維護整個系統

---

## 📑 目錄

1. [系統整體架構](#1-系統整體架構)
2. [每日操作 SOP](#2-每日操作-sop)
3. [專案資料夾結構](#3-專案資料夾結構)
4. [Python Agents（工作員）詳解](#4-python-agents工作員詳解)
5. [Google Sheets 結構](#5-google-sheets-結構)
6. [Portal 網站結構](#6-portal-網站結構)
7. [Apps Script（Portal 後端 API）](#7-apps-script-portal-後端-api)
8. [桌面啟動器](#8-桌面啟動器)
9. [常見維護操作](#9-常見維護操作)
10. [疑難排解](#10-疑難排解)

---

## 1. 系統整體架構

### 1.1 三層架構圖

```
┌─────────────────────────────────────────────────────────┐
│  ① 資料來源層                                            │
│  ┌─────────────────────┐                                │
│  │  科威系統           │ → 每日下載「原始大總表.xlsx」      │
│  │  (旅遊報名平台)     │   放到 DBC base/ 資料夾           │
│  └─────────────────────┘                                │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  ② 處理層（本機 Python）                                 │
│  ┌─────────────────────────────────────────────────┐  │
│  │ main.py --all                                     │  │
│  │   ↓                                               │  │
│  │ CLEANER 清洗員 ─→ COURSE 課程安排寫入員 ─→        │  │
│  │ ─→ WATCHER 比對員 ─→ REPORTER 報表員 ─→            │  │
│  │ ─→ SALES 銷量分析員 ─→ CHECKIN 入住員              │  │
│  │   ↓                                               │  │
│  │ 寫入 Google Sheets / 輸出 JSON / git push        │  │
│  └─────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  ③ 顯示層                                                │
│  ┌──────────────┐  ┌──────────────────────────────┐  │
│  │ Google Sheets│  │ Portal 網站                   │  │
│  │ (內部編輯用) │  │ (員工/教練查看用)             │  │
│  │              │  │ github.io/dbc-base/portal/    │  │
│  └──────────────┘  └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 1.2 資料流向總覽

```
科威 xlsx ──→ CLEANER 清洗 ──→ 寫入 課程安排26/27
                              ↓
                          WATCHER 比對異動
                              ↓
                          異動紀錄 / 野澤訂房比對報告
                              ↓
                          SALES 銷量分析
                              ↓
                          sales_data.json (push 到 GitHub)
                              ↓
                          Portal 銷量分析頁

野澤訂房表(手動編輯) ──→ CHECKIN 入住員 ──→ 寫入 入住單 + 課程安排
教練班表(手動編輯) ────────────────────→ Portal 即時讀取
```

---

## 2. 每日操作 SOP

### 2.1 每天必做

**早上：**

1. 從科威下載最新「原始大總表」（檔名格式：`原始大總表MMDD.xlsx`）
2. 放到 `C:\Users\JW\Desktop\DBC base\` 資料夾
3. 雙擊 `啟動器.bat`
4. 按 **🚀 跑每日流程**（等待約 30 秒）
5. 看到 "🎉 全部完成" 表示 OK

**這個流程會自動跑：**
- 清洗大總表
- 寫入 Google Sheets「課程安排26/27」
- 比對異動 → 寫入「異動紀錄」 + 野澤訂房表「比對報告」
- 計算銷量分析 → 輸出 portal/sales_data.json
- 自動 git push 讓 Portal 更新

### 2.2 安排好野澤訂房後

主管在 Google Sheets「野澤訂房表」填好旅館分配 → 雙擊啟動器 → 按 **🏨 訂房表更新**

這會把旅館名稱寫入：
- 各區的入住單分頁
- 課程安排的「項目」欄

### 2.3 主管編輯完教練班表後

主管在 Google Sheets「26/27教練班表」分頁手動編輯 → 雙擊啟動器 → 按 **🎿 班表更新**

這會通知所有 Portal 使用者強制刷新班表 cache，立刻看到新班表。

### 2.4 流程圖

```
       每天早上
          ↓
   下載科威大總表
          ↓
   放到 DBC base 資料夾
          ↓
   雙擊啟動器
          ↓
   ┌──────────────────────┐
   │  🚀 跑每日流程        │ ← 必跑
   └──────────────────────┘
          ↓
     檢查比對報告
          ↓
   ┌──────────────────────┐
   │ 訂房表編輯（手動）   │
   └──────────────────────┘
          ↓
   ┌──────────────────────┐
   │  🏨 訂房表更新        │ ← 訂房安排完才跑
   └──────────────────────┘
          ↓
       完成 ✓
```

---

## 3. 專案資料夾結構

```
C:\Users\JW\Desktop\DBC base\
│
├── 🎯 主要程式
│   ├── main.py                  # 主控程式
│   ├── launcher.py              # 桌面啟動器 GUI
│   ├── 啟動器.bat               # 雙擊開啟啟動器
│   └── requirements.txt         # Python 套件清單
│
├── 🔧 各個工作員
│   └── agents/
│       ├── cleaner.py           # CLEANER 清洗員
│       ├── course.py            # COURSE 課程安排寫入員
│       ├── watcher.py           # WATCHER 比對員
│       ├── reporter.py          # REPORTER 報表員
│       ├── sales.py             # SALES 銷量分析員
│       ├── checkin.py           # CHECKIN 入住員
│       └── (其他舊版檔案)
│
├── 📚 共用模組
│   └── shared/
│       ├── config.py            # 所有設定/常數
│       ├── gsheet.py            # Google Sheets 操作
│       ├── drive.py             # Google Drive 操作
│       └── utils.py             # 工具函數
│
├── 🌐 Portal 網站檔案
│   └── portal/
│       ├── index.html           # 主頁面（所有 UI 都在這）
│       ├── manifest.json        # PWA 設定
│       ├── sw.js                # Service Worker
│       ├── sales_data.json      # 銷量分析資料（Python 產生）
│       └── coach_version.json   # 班表版本標記
│
├── 🔑 認證檔案
│   └── dbc-credentials.json     # Google 服務帳號金鑰（不可外流）
│
├── 📊 資料檔案
│   ├── 原始大總表MMDD.xlsx      # 當季最新（每日下載）
│   ├── 原始大總表23-24.xlsx     # 歷年資料
│   ├── 原始大總表24-25.xlsx
│   ├── 原始大總表25-26.xlsx
│   ├── 完整大表.xlsx            # 每日輸出（含個資，本機留存）
│   └── cleaner_output.pkl       # agent 間共享資料
│
└── 📝 setup 腳本（一次性）
    ├── setup_user_mgmt.py       # 使用者管理表結構
    └── setup_change_log.py      # 異動紀錄表格式
```

---

## 4. Python Agents（工作員）詳解

### 4.1 CLEANER 清洗員 📥

**檔案：** `agents/cleaner.py`

**何時跑：** 每日流程第一個

**做什麼：**
1. 找 `DBC base/` 內最新的「原始大總表MMDD.xlsx」（排除 YY-YY 格式的歷年檔案）
2. 讀取 Excel：第 2 列是標題、第 3 列起是資料
3. 重複欄位自動加 `_1`、`_2` 後綴（例如「序號_1」「序號_2」）
4. 過濾 `團號='數量小計'` 的合計列
5. 從「團號」推算雪場（ZN=野澤純課、YS=龍平、NT=野澤...）
6. 處理同訂單第 2 人開始團號空白的合併儲存格問題（forward-fill）
7. 輸出 `cleaner_output.pkl` 給其他 agent 用

**關鍵函數：**
- `run()` 主入口
- `_get_area()` 從團號判斷雪場（位於 shared/config.py 的 ITEM_RULES）

**規則：**
- ZN → 野澤純課
- ZB → 斑尾純課
- ZY → 湯澤純課
- YS → 龍平
- NT → 野澤
- BT → 斑尾
- N → 野澤
- B → 斑尾

---

### 4.2 COURSE 課程安排寫入員 📝

**檔案：** `agents/course.py`

**何時跑：** CLEANER 後

**做什麼：**
1. 讀 Google Sheets「課程安排26/27」現有資料
2. 比對 CLEANER 輸出，更新自動欄位（從科威來的）
3. 保留手動欄位（教練、助教、checking、備註、配房序）不覆蓋
4. 套用格式（顏色、欄寬、格線、Filter View）
5. 重建篩選器

**欄位定義（在 config.py 的 COURSE_COLS）：**
```
0  編號          1  項目          2  出發日期
3  泊數          4  天數          5  註記
6  訂編          7  中文姓名      8  英文姓名
9  性別          10 年齡          11 尺寸
12 身高          13 體重          14 腳長
15 雪板          16 LEVEL         17 類別
18 衣褲          19 護膝          20 護臀
21 手套          22 4件組         23 教練(手動)
24 助教(手動)    25 checking(手動) 26 備註(手動)
27 旅客編號(隱藏) 28 項目1(隱藏)   29 配房序(隱藏,手動)
```

---

### 4.3 WATCHER 比對員 🔍

**檔案：** `agents/watcher.py`

**何時跑：** COURSE 後

**做什麼兩件事：**

**Part A：課程安排異動偵測**
- 比較今天 vs 昨天的 課程安排
- 找出：新增、刪減、欄位變動
- 寫入「異動紀錄」分頁
- 重要欄位變動會標紅（Lv、出發日期、類別、衣褲、護具、4件組等）

**Part B：野澤訂房表比對**
- 讀「野澤訂房表」當前訂單
- 跟新資料比對，產出三大類報告：
  1. **新增/刪減訂單**：訂房表沒有的新訂單、或訂房表有但新資料沒有的訂單
  2. **異動**：出發日期、泊數、人數變動
  3. **補資料**：原本是「旅客1/旅客2」佔位 → 新資料有真名了
- 寫入「比對報告」分頁

**關鍵邏輯：**
- 解析併房儲存格：5542 5561 同一格用 regex 取出
- 訂房表人數 = 「大人」+「小人」+「嬰兒」三欄相加
- 出發日期排序 + 交替底色顯示

---

### 4.4 REPORTER 報表員 📊

**檔案：** `agents/reporter.py`

**何時跑：** WATCHER 後

**做什麼：**
- 過去產生 HTML 報表，現已**全部停用**（已被 Portal 頁面取代）
- 保留 agent 框架未來可能用到

---

### 4.5 SALES 銷量分析員 💰

**檔案：** `agents/sales.py`

**何時跑：** REPORTER 後

**做什麼：**
1. 讀當季 + 所有歷年大總表
2. 計算統計：
   - 總人次、營收、各區人數
   - 每日新增報名
   - 出發人次分布
   - 客群（性別、單雙板、Level、年齡）
   - 加購項目（續滑、專屬、租借）
3. 跨年回頭客分析：
   - 用 union-find 演算法跨年聚合同人
   - 比對 key：身分證 / 護照 / 中文姓名+生日（任一相同 = 同人）
   - 個資留在 Python 端不上 JSON
4. 同期 YoY 比較：跟每個歷年同期累積、同日出發人次比對
5. 輸出 `portal/sales_data.json`（純統計、無個資）

**輸出檔：** `portal/sales_data.json`（約 260 KB）

**特別規則：**
- 23/24 雪季全部歸野澤（當年只有野澤一個雪場）
- 5/1 為開賣日 day 0

---

### 4.6 CHECKIN 入住員 🏨

**檔案：** `agents/checkin.py`

**何時跑：**
- 在 `--all` 流程末端自動跑一次
- 訂房表填好後可單獨跑：`python main.py --checkin`

**做什麼：**

**Step 1：讀「野澤訂房表」**
- 取得每張訂單的旅館分配

**Step 2：寫入「入住單」**
- 野澤入住單26/27、斑尾入住單26/27、龍平入住單26/27
- 把旅館名稱寫到「項目」欄
- 保留手動欄位（房型、入住備註、退房備註）不覆蓋
- 加日期分隔列 + 旅館人數小計

**Step 3：回寫「課程安排」**
- 同訂單所有旅客都更新旅館名稱（不只第一個）

---

## 5. Google Sheets 結構

### 5.1 主要試算表

| 試算表 | 用途 | ID |
|---|---|---|
| **DBC Base** | 主資料庫 | `12p71jgMErzZYO4toU2LVELDPfwklHCyDARclldYImCc` |
| **野澤訂房表** | 旅館訂房（獨立） | `1eNXXQgCEqHxd2VmzbGe3Z-Gac2PNBgQlSAKx19uqQ6s` |
| **使用者管理** | Portal 帳號權限 | `1b2YR6VpDT_KYOExWLw0Gsy9D0Ise-kBcKtVDW8zrfPM` |

### 5.2 DBC Base 內的分頁

```
DBC Base 試算表
├── 課程安排26/27         ← 主資料表（Python 自動更新）
├── 異動紀錄              ← WATCHER 自動寫入
├── 26/27教練班表         ← 主管手動編輯
├── 野澤入住單26/27       ← CHECKIN 自動更新
├── 斑尾入住單26/27       ← CHECKIN 自動更新
├── 龍平入住單26/27       ← CHECKIN 自動更新
└── (送客單分頁已刪除，改由 Portal 從入住單推算)
```

### 5.3 野澤訂房表

```
野澤訂房表
├── 26-27 (gid=2053936646)    ← 主管手動編輯訂房分配
└── 比對報告                  ← WATCHER 自動寫入
```

**欄位順序：** A 出發日期 / B 曜日 / C 訂編 / D 中文姓名 / E 英文姓名 / G 泊數 / H 部屋 / I 大人 / J 小人 / K 嬰兒 / ...

**重要：** 訂編欄位是合併儲存格（同訂單只在第一列出現）

### 5.4 使用者管理試算表

```
使用者管理試算表
├── 使用者清單               ← 帳號審核 + 教練名綁定
│   A: Email
│   B: 姓名
│   C: 角色
│   D: 狀態（核准/待審核/停用）
│   E: 註冊時間
│   F: 最後登入時間
│   G: 登入來源
│   H: 教練名（綁定教練班表的名字，給「我的班表」用）
│
└── PORTAL 頁面權限          ← 哪個角色能看哪個頁面
    各角色 × 各頁面 填 O 代表有權限
```

---

## 6. Portal 網站結構

### 6.1 網址
**https://dbcjimmyoklay.github.io/dbc-base/portal/**

### 6.2 託管方式
**GitHub Pages（免費）**
- repo: https://github.com/dbcjimmyoklay/dbc-base
- 任何 push 到 main branch 的變動都會自動部署（約 1-2 分鐘）

### 6.3 主檔案
**`portal/index.html`** — 整個 Portal 都寫在這一個檔案（約 4000 行）：
- 上半段：CSS 樣式
- 中段：HTML 結構（登入 / 註冊 / 待審核 / 主畫面）
- 下半段：JavaScript（每個頁面的渲染邏輯）

### 6.4 Portal 頁面清單

```
個人專區
  ├── 個人資料         (placeholder, 未實作)
  ├── 我的班表         (日曆模式，依 email 對應教練名)
  └── 零用金           (placeholder, 未實作)

課程
  └── 課程安排         (Airtable 風格)

入住單
  ├── 入住單－野澤
  ├── 入住單－斑尾
  └── 入住單－龍平

送客單
  ├── 送客單－野澤
  ├── 送客單－斑尾
  └── 送客單－龍平
     （資料來源就是入住單，計算退房日期 = 出發日期 + 泊數）

管理
  ├── 教練班表         (完整矩陣，保留 Sheet 字體顏色)
  ├── 每日教練需求     (列表/月曆 + 4 級警示)
  └── 銷量分析         (限定老闆/系統創建者)
```

### 6.5 PWA 功能
Portal 可以「加入主畫面」變 App。設定在：
- `portal/manifest.json`
- `portal/sw.js`（Service Worker，network-first 策略）

---

## 7. Apps Script（Portal 後端 API）

### 7.1 是什麼
Google Apps Script 跑在「使用者管理試算表」上，作為 Portal 的 API 後端。

### 7.2 部署網址
`https://script.google.com/macros/s/AKfycbw2-7v48isKdkrJyZNCJEI1UEFfbL3lupGZwUPO4vHyt9B3AjkxKaV3ZHQlvz_LenOI/exec`

### 7.3 提供的 API

| Action | 功能 |
|---|---|
| `register` | 新使用者註冊 |
| `getToday` | 今日出發人數 |
| `getCourse` | 課程安排資料 |
| `getCheckin?area=` | 入住單資料 |
| `getCheckout?area=` | 送客單（已停用，Portal 改用 getCheckin 推算） |
| `getCoach` | 教練班表資料（含字體顏色） |

### 7.4 如何修改
1. 打開「使用者管理」試算表
2. 上方 **「擴充功能」→「Apps Script」**
3. 編輯 Code.gs
4. 儲存（Ctrl+S）
5. **Deploy → Manage deployments → Edit → Version 選 New version → Deploy**

⚠️ **重要：** 每次改完 Apps Script 都要重新部署才會生效

### 7.5 認證機制
- 用 Google ID Token 驗證（透過 Google Sign-In）
- 第一次登入後，Apps Script 用 HMAC 簽章 email 產生「userKey」
- 後續所有 API 改用 userKey（永久有效，除非管理員停用該帳號）
- 解決了之前每小時要重新登入的問題

---

## 8. 桌面啟動器

### 8.1 開啟方式
雙擊 `啟動器.bat`

### 8.2 5 顆按鈕

| 按鈕 | 對應動作 |
|---|---|
| 🚀 跑每日流程 | `python main.py --all` + 自動 git push |
| 🌐 開啟 Portal | 開瀏覽器到 Portal 網址 |
| 📊 開啟 DBC Base | 開瀏覽器到主試算表 |
| 🎿 班表更新 | 更新 coach_version.json + git push（強制 Portal 刷新班表 cache） |
| 🏨 訂房表更新 | `python main.py --checkin`（比對訂房表 → 更新入住單與課程安排） |

### 8.3 程式碼
`launcher.py` — Python tkinter GUI

修改按鈕：在 `_build_ui()` 函數內找到 `self._mkbtn(...)` 即可改。

---

## 9. 常見維護操作

### 9.1 新增 Portal 使用者

1. 該員工先用 Google 帳號到 Portal 登入
2. 系統會自動進入「等待審核」狀態
3. 打開「使用者管理」試算表 → 「使用者清單」分頁
4. 找到該員工的列，把「狀態」改成 `核准`
5. 「角色」欄填角色名稱（例如「教練」「管理員」「老闆」）
6. 該員工重新整理 Portal 即可進入

### 9.2 給新角色頁面權限

1. 「使用者管理」試算表 → 「PORTAL 頁面權限」分頁
2. 找該角色那一列
3. 想開哪個頁面就在對應欄位填 `O`
4. 用戶下次登入就會看到

### 9.3 綁定教練名稱（讓「我的班表」work）

「使用者清單」→ 該教練的列 → **H 欄填寫教練名**
（要跟教練班表上的名字完全一致，包含大小寫和空格）

### 9.4 修改 Portal 頁面樣式 / 功能

1. 編輯 `portal/index.html`
2. 跑 `git add portal/index.html && git commit -m "說明" && git push`
3. 等 1-2 分鐘 GitHub Pages 自動部署
4. 強制重新整理瀏覽器（Ctrl+Shift+R）

### 9.5 修改某個 agent 邏輯

1. 編輯對應 `agents/xxx.py`
2. 測試：`python main.py --xxx`（例如 `--checkin`、`--sales`）
3. 確認無誤後跑 `python main.py --all` 完整測試
4. `git push` 上傳

### 9.6 新增銷量分析的歷年資料

1. 把新的「原始大總表YY-YY.xlsx」放到 `DBC base/` 資料夾
2. 跑 `python main.py --sales`
3. 自動 `git push`

---

## 10. 疑難排解

### 10.1 啟動器跑「跑每日流程」失敗

**檢查 1：** 是否有「原始大總表MMDD.xlsx」檔？
- 從科威下載新的，放到 `DBC base/`

**檢查 2：** 看輸出視窗的紅字錯誤訊息

**檢查 3：** Google 服務帳號權限
- 文件：`dbc-credentials.json`
- 服務帳號 email: `dbc-sheet@gen-lang-client-0187113768.iam.gserviceaccount.com`
- 確認該帳號在試算表「共用」清單裡有「編輯者」權限

### 10.2 Portal 看不到資料

**檢查 1：** 強制重新整理（Ctrl+Shift+R）

**檢查 2：** 清除 cache
- Portal 左下角「↺ 清除快取」按鈕

**檢查 3：** Apps Script 是否重新部署
- 改完 Apps Script 必須 Deploy → New version

### 10.3 「我的班表」顯示「尚未綁定教練名稱」

到「使用者管理」→「使用者清單」→ 該教練的 H 欄填教練名。

### 10.4 WATCHER 比對報告寫入失敗（403）

服務帳號沒有野澤訂房表的編輯權限。
1. 打開野澤訂房表
2. 右上「共用」→ 加入 `dbc-sheet@gen-lang-client-0187113768.iam.gserviceaccount.com` 為**編輯者**

### 10.5 銷量分析數字不對

- 檢查歷年大總表檔名格式：必須是 `原始大總表YY-YY.xlsx`（例如 `原始大總表25-26.xlsx`）
- 23/24 雪季強制歸野澤（當年只有野澤）
- 5/1 為開賣日 day 0

### 10.6 git push 失敗

```
git pull
（解決衝突）
git push
```

如果出現 token 過期，要重新登入 GitHub。

---

## 📞 程式碼來源

GitHub: https://github.com/dbcjimmyoklay/dbc-base
所有變更歷史都在 git log 可以查。

---

## ⚙️ 環境設定（換電腦要重裝時）

```bash
# 1. 安裝 Python 3.13
# 從 https://www.python.org 下載

# 2. 安裝套件
pip install -r requirements.txt

# 3. 拿到 GitHub repo
git clone https://github.com/dbcjimmyoklay/dbc-base.git

# 4. 把 dbc-credentials.json 放到 DBC base/ 資料夾根目錄
#    （這個檔案是 Google 服務帳號金鑰，不在 git 裡）

# 5. 雙擊 啟動器.bat 開始用
```

---

## 🔑 重要憑證（不要外流）

- **Google 服務帳號金鑰**：`dbc-credentials.json`
- **Google OAuth Client ID**：在 portal/index.html 內（公開的沒問題）
- **GitHub 帳號**：dbcjimmyoklay

---

## ✏️ 文件結束

如有疑問可看 git commit 訊息了解歷史變更，或聯絡前任維護者。
